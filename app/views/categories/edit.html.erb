<h1>Edit Category</h1>

<%= form_for @category do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>

<%= link_to 'Back', categories_path %>
